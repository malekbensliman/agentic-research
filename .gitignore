# OS
.DS_Store
Thumbs.db

# Claude Code — personal/local settings (the committed settings.json is intentional; see .claude/)
.claude/settings.local.json

# Rendered slide decks (build outputs; markdown is the source of truth — final decks live in Google Drive)
*.pptx
# the slim CBS reference template IS tracked (source asset, ~92 KB)
!templates/cbs-template.pptx
slides/**/*.pdf
/build/

# Env / secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# When Words Sweat — raw source data (240–355 MB each). The reduced, shareable
# dataset under activities/when-words-sweat/ IS tracked; only the big roots are ignored.
/prosper_archive_processed_with_compettion_*.csv

# Visual companion (brainstorm scratch — mockups persist here but aren't curated)
.superpowers/
