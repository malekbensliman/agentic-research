# OS
.DS_Store
Thumbs.db

# Claude Code — personal/local settings (the committed settings.json is intentional; see .claude/)
.claude/settings.local.json

# Rendered slide decks (build outputs; markdown is the source of truth — final decks live in Google Drive)
*.pptx
!templates/cbs-template.pptx   # the slim CBS reference template IS tracked (source asset, ~92 KB)
slides/**/*.pdf
/build/

# Env / secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Visual companion (brainstorm scratch — mockups persist here but aren't curated)
.superpowers/
