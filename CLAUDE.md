# CLAUDE.md

@AGENTS.md

## Claude-only notes

- This repo's portable context lives in `AGENTS.md` (imported above) so it works for any agent; add Claude-specific guidance below.
- Slide content is markdown (source of truth); `.pptx` is a render target — edit the `.md`, not the deck.
- This repo ships an intentionally elaborate `.claude/settings.json` (Explanatory output style, the CBS marketplace, sensible defaults) because faculty using it are not assumed to have a personal dotfiles setup.
