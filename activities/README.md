# activities/

Sample repositories faculty point Claude Code at during hands-on exercises.

- Includes a **fallback sample project** so anyone without their own data (or with IRB-sensitive data) still has something to work on.
- The headline exercise is bring-your-own-project: open a folder of an existing project in VS Code and ask Claude to summarize it — so faculty can see exactly which files Claude has access to.
