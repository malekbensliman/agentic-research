# onepager/

A **one-page cheat sheet** for faculty: short definitions of the key jargon and the most useful commands.

Kept in sync with / generated from the slide content (see `../scripts/`) so definitions and commands never drift from what's taught. Source is markdown; the distributable is a Word `.docx` / `.pdf`.
